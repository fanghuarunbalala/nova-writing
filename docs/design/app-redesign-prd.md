# Novel 创作台 · 前端整体重设计 PRD

| 项 | 内容 |
| --- | --- |
| 版本 | v1.1（对齐 demo 终版；含决议：最小窗口 1080，开放问题已定案见 §17） |
| 日期 | 2026-08-15 |
| 状态 | 待评审 |
| 规范来源 | `docs/design/app-redesign-demo.html`（交互 demo，唯一视觉/交互基准） |
| 回归脚本 | `docs/design/.demo-smoke.cjs`（`node .demo-smoke.cjs`，56 项断言） |
| 主题基准 | `docs/design/theme-candidates-demo-2.html`（四主题 token 体系） |

> 本文所有尺寸、文案、状态、断点均以 demo 终版为准；demo 与本文冲突时**以 demo 为准并回改本文**。
> 需求编号规则：TB 标题栏 / WC 窗控 / WS 工作区 / ST 设置 / SB 侧栏 / CH 对话 / CV 内容（OL 大纲 · MS 正文 · PM 人物 · PL 地点）/ PN 计划 / AP 审批 / RWD 响应式 / TH 主题 / AN 动效 / A11Y 无障碍。

---

## 1. 背景与目标

现有 GUI（`ui/src`，ApplicationShell 三视图 + 全局侧栏 + 右侧 Inspector）功能已齐备，但布局信息密度不均、目录结构不清晰、窗口控制依赖系统原生标题栏。本次重设计在**不改变既有数据模型与领域 store** 的前提下，重排信息架构：

1. **目录清晰**：侧栏随视图切换内容（对话=会话目录、内容=资料目录、计划=安排目录），层级树状呈现；
2. **平台一致的窗口体验**：自绘 Windows / macOS 窗口控制，标题栏即导航栏；
3. **审批闭环可视**：审批面板与对话绑定，一批审批卡片化，变更前后对照可见；
4. **全窗口尺寸可用**：断点 1280 / 1080 两档降级路径明确，最小窗口 1080；
5. **四主题零成本切换**：沿用 `data-theme` 覆盖 L3 色层机制。

## 2. 范围

### 2.1 本期范围
应用外壳（标题栏 / 视图切换 / 窗控 / 工作区 / 设置）、三视图布局与目录、对话输入区、审批面板、主题与响应式。对应 demo 全部可见元素与交互。

### 2.2 非目标（含已决议排除项）
- ~~侧栏 / 审批面板的拖拽调宽：**已决议移除** DragHandle，宽度采用断点固定档位（§17 决议 2）~~ → **v0.8 修订（§17 决议 7）**：右栏（内容目录）恢复拖拽调宽（320–640、双击复位，≤1280 仍固定档）；侧栏维持固定档；
- 更窄（≤860）移动级布局：**已决议不做**，应用最小窗口 1080（§17 决议 6）；
- 侧栏会话搜索的真实过滤、会话右键/更多菜单（重命名/置顶/删除）的真实交互（demo 仅占位）；
- 设置「通用」页真实持久化（demo 为样式占位）；
- InspectorHost 中 档案 / 大纲单元 / 会话元信息 等非审批面板的重新设计（demo 未覆盖，见 §17）；
- 底部「demo 控制条」（主题/平台/窗宽/运行状态/说明）为演示与 QA 工具，**不属于产品需求**。

---

## 3. 信息架构

### 3.1 视图模型
- 主视图状态：`chat | content | plan`（沿用 `MainViewRouter` 双栈历史，支持返回）。
- 内容视图子位：`outline | manuscript | characters | locations`（沿用 `contentTab`）。
- 侧栏为**上下文目录**：内容由当前主视图决定，非全局混排（**与现状的核心差异**）。
- 布局骨架：

```
┌─ 标题栏 46px（窗控 · 品牌 · 工作区 · [对话|内容|计划] · 工作区 · 设置 · 窗控）─┐
├─ 侧栏（随视图切换） ─┬─ 主区（subHead + 内容） ─┬─ 审批面板（仅对话视图） ─┤
└──────────────────────────────────────────────────────────────┘
```

### 3.2 跨视图导航（demo 已实现，必须保留）
| 动作 | 起点 | 终点 |
| --- | --- | --- |
| 内容/计划 subHead 返回箭头 | 内容/计划 | 对话 |
| 大纲单元详情「在正文中查看」 | 大纲 | 正文对应章节 |
| 档案「关联单元」chip | 人物/地点/大纲 | 大纲对应单元（选中） |
| 待办「去审批」 | 计划 | 对话 + 打开审批面板 |
| 待办「去完善」 | 计划 | 内容·地点（定位到该地点） |
| 待办「去处理」 | 计划 | 内容·正文（定位到该章） |
| 待办「去讨论」 | 计划 | 对话 |
| 对话流审批系统消息 | 对话 | 打开审批面板 |
| 计划·大纲进度行 | 计划 | 内容·大纲（定位到该单元，toast「已定位到大纲：…」） |

---

## 4. 全局外壳

### 4.1 标题栏（TB）
| 编号 | 需求 |
| --- | --- |
| TB-1 | 高度 46px，底边框 1px，`--color-chrome-bg` + 12px backdrop-blur；整栏 `-webkit-app-region: drag`（Electron frameless 预留），交互元素 no-drag。 |
| TB-2 | 左→右：平台窗控（仅 mac）· 侧栏开关（PanelLeft 图标）· 品牌（渐变圆点 13px + 「Novel」粗体，圆点 grad-flow 6s 流动）· 工作区 chip · 中央视图切换器 · 右簇（工作区 FolderOpen 图标钮、设置 gear 图标钮、Win 窗控）。 |
| TB-3 | 图标钮规格 28×28、radius 6px；hover `accent-9` 底 + `accent-ink` 字；focus-visible 焦点环 `--color-focus-ring`。 |
| TB-4 | 侧栏开关切换侧栏显隐；标题栏空白处**双击** = 最大化/还原切换（Electron 惯例）。 |

### 4.2 视图切换器
| 编号 | 需求 |
| --- | --- |
| TB-5 | 三段等宽 grid（`repeat(3, 1fr)`，padding 2 / gap 2 / radius 6），`surface-2` 底 + `shadow-xs`；每钮 = 图标 13px + 文案 12px（对话 messageSquare / 内容 bookOpen / 计划 calendarDays）。 |
| TB-6 | 选中态由滑块 thumb 表达：`surface` 底 + `shadow-xs`，位移 `translateX(calc(i * (100% + 2px)))`，0.22s `ease-out`；选中文字 `accent-ink`。 |
| TB-7 | `aria-selected` 表达选中；`role="tablist"`。 |

### 4.3 工作区（WS）
| 编号 | 需求 |
| --- | --- |
| WS-1 | 工作区 chip：FolderOpen 图标 + 当前工作区名 + 下拉箭头；hover `accent-9`；最大宽 220px 截断。点击（或右簇同名图标钮）弹出工作区菜单。 |
| WS-2 | 工作区菜单：min-width 240、blur 14、`shadow-2`、view-in 0.18s；分组「当前工作区」（选中项 check · accent 色）→ 最近项目列表（FolderOpen 图标 + 名称 + 副信息「单元/字数/时间」）→ 分隔线 → 「打开其他项目…」。 |
| WS-3 | 切换最近项目：chip 更新 + toast「已切换到工作区：X」；点击菜单外任意处关闭。 |

### 4.4 窗口控制（WC）
| 编号 | 需求 |
| --- | --- |
| WC-1 | **Windows**：右缘三钮，各宽 44px、通高：最小化（−）/ 最大化·还原（方框/双框图标切换）/ 关闭（×）。hover：`fg 8%` 底；**关闭 hover `#e81123` + 白色图标**。 |
| WC-2 | **macOS**：左缘红绿灯 12px 圆点（`#ff5f57 / #febc2e / #28c840`，间距 8，容器含左 padding）；hover 组时显示内符号（× − ＋）。 |
| WC-3 | 平台差异由 `data-platform` 属性驱动，运行时按 `process.platform` 设置（demo 由控制条切换）。两平台互斥：win 不渲染红绿灯、mac 不渲染 winCtl。 |
| WC-4 | 行为：最小化→最小化到任务栏/Dock；最大化/还原→图标互换 + 状态记忆；关闭→退出应用（demo 以「应用已退出（演示）」遮罩 + 重新打开按钮模拟）。 |
| WC-5 | 实现前提：Electron `frame:false`（mac 可 `titleBarStyle:"hidden"` 保留红绿灯），窗口控制经 IPC 调 `minimize/maximize/unmaximize/close`；标题栏拖拽区见 TB-1。 |

### 4.5 设置弹窗（ST）
| 编号 | 需求 |
| --- | --- |
| ST-1 | 遮罩 `fg 22%`；弹窗 `min(560px, 92vw)`、radius 14、`shadow-2`、view-in 0.25s；头部「设置」+ 关闭钮。 |
| ST-2 | 左侧标签列 108px：**通用 / 外观 / 关于**（`aria-selected`，选中 `accent-9`）。点击遮罩空白关闭，点击弹窗内不关闭。 |
| ST-3 | 外观页：四主题卡 grid（`minmax(150px,1fr)`）：四色色条 + 名称 + 描述；选中卡 accent 描边 + 3px 焦点环（`aria-pressed`）。切换即应用 `<html data-theme>`（全局 0.35s 颜色过渡）并持久化。 |
| ST-4 | 通用页（本期样式占位）：开关行 = 标题 + 副说明 + toggle（34×20 胶囊，选中 accent，滑块 0.22s 位移）；「关于」页含版本与断点说明。 |

---

## 5. 侧栏 · 上下文目录（SB）

侧栏随主视图切换内容（SB-1）；宽度与显隐见 §11。

### 5.1 对话视图 · 会话目录（SB-2～4）
| 编号 | 需求 |
| --- | --- |
| SB-2 | 顶部「开始一段新的创作」主按钮：品牌渐变底 + 白字（on-accent）+ `accent-45` 投影，hover 上浮 1px。 |
| SB-3 | 搜索框：search 图标 + 占位「搜索对话…」（`surface` 底、border、radius 6）。 |
| SB-4 | 会话分组标签：**置顶 / 今天 / 更早**（11px faint）；会话行 = 图标位 27px（accent-8-t 底 accent-ink 图标）+ 标题（12.5 medium，置顶行带 pin 小图标）+ 副信息（当前章节/条数/字数）+ 右缘状态点或更多钮。 |
| SB-4a | 状态点语义：生成中 `warn` / 失败 `danger` / 完成 `success` / 无状态→更多（⋯）钮。选中行 `accent-9` 底 + accent 18% 描边。 |

### 5.2 内容视图 · 资料位（SB-5～9）
| 编号 | 需求 |
| --- | --- |
| SB-5 | 顶部四段 tab（grid×4，`surface-2` 容器 + 阴影，选中 `surface` 浮起）：**大纲（listTree）/ 正文（scrollText）/ 人物（userRound）/ 地点（mapPin）**，图标在上、名称在下，附计数徽标；等价于 `contentTab`，点击切换左侧目录与主区。 |
| SB-6 | 目录头：名称 + 计数胶囊（mono · `accent-11` 底）+ 右侧工具（大纲：展开全部/折叠全部 iconBtn）。 |
| SB-7 | **大纲树（层级/树状，硬性要求）**：卷（ARC）节点带折叠箭头（chevron 90° 旋转 0.18s），点击箭头仅折叠、点击行选中；子级以嵌套容器逐层缩进（缩进 9px + 7px）并画**竖向引导线**（`border-strong 60%`）；章（SCENE）叶节点用 7px 短横线与引导线相连；行右侧 = 进度分数（mono，如 2/5）+ 实现状态 chip；卷行 label 加粗；底部图例陈列全部状态 chip。 |
| SB-8 | **正文目录**：卷组（chevron + 卷名 + 章数 mono）可折叠；章行 = 状态点 + 标题 +（有草稿时）「草」warn 胶囊；弃置章标题划线、faint。状态点色：完成 `success` / 撰写中 `warn` / 待写 `border-strong` / 受阻 `danger` / 弃置 `faint`。 |
| SB-9 | **人物目录**：26px 圆形首字头像（`accent-11` 底）+ 姓名 + 角色（faint 小字）。**地点目录**：mapPin 图标位 + 名称 + 现状（locState，faint 小字）。选中态同 SB-4。 |

### 5.3 计划视图 · 安排目录（SB-10）
| 编号 | 需求 |
| --- | --- |
| SB-10 | 首行「总览」（layers 图标 + 副信息「统计 · 双状态轴 · 大纲进度」）+ 分组：**待审批（warn 点 + warn 计数）/ 档案 / 写作 / 决策** + **已完成**；待办行 = 空心圆点 + 标题 + 分组图标；已完成行圆点实心 `success` + check、文字 faint。空分组不渲染。 |

---

## 6. 对话视图（CH）

### 6.1 头部
| 编号 | 需求 |
| --- | --- |
| CH-1 | subHead：作品名（粗体）+ kicker（当前章节 · 轮次，如「第 2 章 · 追逃段修订 · 第 3 轮」）+ 右侧 置 pin / 更多 iconBtn。 |

### 6.2 对话流
| 编号 | 需求 |
| --- | --- |
| CH-2 | 滚动容器居中列 **max-width 760px**；贴底跟随（用户上滚 >48px 暂停跟随，沿用现网）。 |
| CH-3 | **轮次分隔条**：左右细线 + 居中 11px 文案（「第 N 轮 · 标题」）。 |
| CH-4 | **用户消息**：右对齐气泡，max-width 76%，`accent-9` 底 + accent 14% 描边，圆角 `14/14/4/14`；15.5px；进场 msg-enter 0.22s。 |
| CH-5 | **助手消息**：无头像；含 工具行 + 正文 + 草稿面板；max-width 94%。 |
| CH-6 | **工具行（三态，沿用定稿方案 A）**：完成 = ✓（success 图标）+ muted 文字、**不显耗时**、nowrap；失败 = ✕ + `danger` 文字；进行中 = 唯一强调：warn spinner + fg medium 文字 + 整数秒（mono，node-pulse 2.4s 脉动）。 |
| CH-7 | **正文体**：`font-body` 衬线 15.5px / 行高 1.85；引用块 `font-kai` + accent 左线 + `accent-8-t` 底。 |
| CH-8 | **正文草稿面板**：品牌渐变描边（三色 border-box）+ kicker「正文草稿 · 第N章」（渐变文字 + 流动）+ 衬线 15.5/1.95 正文 + 流式末尾闪烁光标（1.1s steps）。 |
| CH-9 | **系统审批消息**：居中胶囊（warn 底 + warn 32% 描边 + shieldCheck 图标），文案「角色写入 · 沈砚 待审批 —— 点击查看」；点击打开审批面板；hover warn 40% 外圈。 |

### 6.3 运行状态行（GenStatus）
| 编号 | 需求 |
| --- | --- |
| CH-10 | 位于输入框上方，`0fr↔1fr` grid 收展（0.3s），高度恒占位防跳变。优先级 failed > waiting > generating。 |
| CH-11 | **正在生成**：feather 笔尖 1.5s 上下浮动 + 墨滴 1.2s 下落 + 品牌渐变流动文字 + 整数秒（tabular-nums，1s 粒度）。 |
| CH-12 | **正在审批**（挂起态文案，**固定为此四字**）：hourglass 沙漏 2.8s 左右摇摆 + `grad-warn-text` 琥珀渐变文字 + 秒数。触发条件：当前会话存在 pending 审批。 |
| CH-13 | **生成失败**：alert 三角一次性 shake 0.45s + 「生成失败」danger 粗体 + 原因（省略号截断）+ 重试圆钮（22px，hover 实心红上浮）；点击重试回到生成态。 |

### 6.4 输入区与底栏
| 编号 | 需求 |
| --- | --- |
| CH-14 | 输入卡 `surface` 底 + `border-strong` 描边 + `shadow-1`；focus-within：accent 描边 + 3px `accent-8-t` 外圈。结构自上而下：状态行（CH-10）→ 输入框 → **底栏**。 |
| CH-15 | 输入框：textarea 自动长高，min 36 / max 140px；占位「输入创作指令… （Tab 引用 角色 / 大纲 / 正文）」；Enter 发送 / Shift+Enter 换行 / **忽略中文输入法组合态**。 |
| CH-16 | **底栏（模式在最左、发送在最右）**：模式钮（见 CH-17）+「待生效」chip + 弹性空隙 + 暂停钮 + 发送钮。 |
| CH-17 | **执行模式 = 点击弹出下拉列表**（对齐现网 ComposerModeBar）：模式钮 = 图标 + 当前模式名 + 箭头（展开时 180° 旋转、钮体 `accent-9`）；菜单**向上弹出**（`bottom: calc(100%+8px)`，min-width 272，blur 14，view-in 0.18s）；每项 = 20px 图标 + 名称（12.5 粗）+ 说明（11 faint）；当前项 `accent-9` 高亮。 |
| CH-18 | 模式三选：**需审核**（shieldCheck，「工具调用与档案写入均生成审批，逐项确认后执行」）/ **直接执行**（zap，「不经审批直接执行工具，信任较高时使用」）/ **设计**（feather，「产出设计草稿，不直接改动档案与正文」）。模式为**会话级权威状态**（`mode.set` 控制事件）。 |
| CH-19 | **待生效 chip**：选择了不同模式后显示 `clock + 待生效`（**不带模式名**）；随下一条消息发出时生效（发送时应用并清除）；再次点选同一待选模式可取消。点击菜单外任意处关闭菜单。 |
| CH-20 | **发送 / 暂停**：发送钮 30×30 品牌渐变 + 白图标；**生成中**：发送隐藏、暂停显示（warn 14% 底 + warn 35% 描边，hover 实心 warn）；**审批挂起**：发送禁用（55% 透明）。 |
| CH-21 | 底部提示行（10.5 faint）：「Enter 发送 · Shift+Enter 换行 · 执行模式对整个会话生效」。 |
| CH-22 | **AskUserQuestion 流内提问卡**：模型需要作者决策 / 澄清 / 采集创意时，提问组卡内联在对话流末尾（**不弹窗、不占右栏**——与审批弹窗的分工：审批管写操作放行，提问管创作输入）。一次 1-4 问混排（单选 / 多选 / 开放填空），逐问折叠、题目突出（完整题目行），选项带推荐徽标与 markdown 描述，开放题带 context 引导块 + placeholder；作者作答（选 / 「其他」自填 / 跳过）后卡片**原地留痕为「题目 → 答案」简约记录**（时间线即审计，journal 重放同位置重建），工具行由运行态（spinner · 等待作答）转完成态（✓ · 已作答），生成继续。提问挂起时发送禁用、状态行显示「等待作答」（纯提问时；与审批并存时维持可点胶囊）。通知中心聚合「作者待作答」条目。 |

---

## 7. 内容视图（CV）

### 7.0 通用
| 编号 | 需求 |
| --- | --- |
| CV-1 | subHead：返回箭头（→对话）+ 资料位名 + kicker + **当前选中项**（subCtx，如章节全名 / 人物名·角色）+ 右侧动作区。动作区按位配置：大纲「展开全部 / 新建单元」、正文「复制正文」、人物「新建角色」、地点「新建地点」。 |
| CV-2 | 内容区 `paneInner`：max-width 880 居中，`view-in 0.3s` 进场；`contentTab` 切换时重挂载。 |
| CV-3 | 卡片块通用规格：`surface` 底 + border + `shadow-1` + radius 9；块标题 12px muted 粗体 letter-spacing 0.04em。 |
| CV-4 | 关联 chip：accent 22% 描边 + `accent-8-t` 底 + `accent-ink` 图标文字，hover `accent-11`；点击跨视图定位（§3.2）。 |

### 7.1 大纲（OL）
| 编号 | 需求 |
| --- | --- |
| OL-1 | 左侧树见 SB-7；右侧为**选中单元详情**。 |
| OL-2 | 详情头：scope chip（卷=accent / 章=neutral）+ 单元名（17px 粗）+ mono 副行（unit id · depth · 父单元名）。 |
| OL-3 | 状态行：规划 chip（构想 neutral / 成纲 info / 备写 accent）+ 实现 chip（待写 neutral / 撰写中 warn / 已完成 success / 受阻 danger / 弃置 faint）+（有子单元时）120px 渐变进度条 + 「c / t 子单元完成」。 |
| OL-4 | 受阻：warn 横幅「受阻：{blockedReason}」；弃置：faint 横幅「已弃置：{abandonedReason}」。 |
| OL-5 | 简介正文；无简介显示「（尚未填写单元简介）」。 |
| OL-6 | 动作：「在正文中查看」（有关联章节时，跳正文并选中）/「在对话中讨论」（跳对话）。 |
| OL-7 | 关联区：关联角色 chip（跳人物）+ 子单元 chip（跳子单元）；无关联显示「无关联条目」。 |

### 7.2 正文（MS）
| 编号 | 需求 |
| --- | --- |
| MS-1 | 左侧目录见 SB-8；右侧为选中章阅读区。 |
| MS-2 | 章头：衬线 18px 章名 + 状态 chip（撰写中/已完成）+ mono 元信息行（卷名 · 字数 · 更新时间 · 含草稿）。 |
| MS-3 | 正文块：`font-body` 16.5px / 行高 1.95 / 首行缩进 2em / 两端对齐。 |
| MS-4 | **草稿块**：warn 3px 左线 + warn 6% 底色 + 尾部「草稿 · 未转入正式稿」标签（feather 图标）。 |
| MS-5 | **受阻章**：warn 横幅「本章受阻：{原因}」+ 楷体空态「等上游定稿，这一章就能落笔。」；**弃置章**：faint 横幅；**未开笔章**：楷体空态「此章尚未落笔——从一句话开始，让故事自己生长。」。 |

### 7.3 人物（PM）/ 地点（PL）
| 编号 | 需求 |
| --- | --- |
| PM-1 | 档案头：46px 圆形首字头像 + 姓名（18 粗）+ kicker（角色定位 · vN 版本）+ 右侧「编辑」次级钮。 |
| PM-2 | 卡片顺序：**简介 / 初始状态 / 作者备注（楷体 muted）/ 关联单元**；关联为 chip 列表，空时「尚未关联」。 |
| PL-1 | 同 PM；kicker 含**现状**（locState，如「地点 · v2 · 现状：雨夜亮灯」）；地点关联为空时提示「尚未关联 —— 计划视图有待办提醒」（呼应待办闭环）。 |

---

## 8. 计划视图（PN）

### 8.1 总览
| 编号 | 需求 |
| --- | --- |
| PN-1 | subHead：「计划 · 总览」+ kicker「待办 + 大纲进度 · 规划 / 实现双状态轴」+ 刷新 ghost 钮。 |
| PN-2 | **统计卡** grid（`minmax(128px,1fr)`）6 张：mono 21px 数字 + 名称 + 注释；数字默认 `accent-ink`，「已完成单元」卡为 warn 变体（数字 warn），注释携带异常提示（如「1 受阻 · 1 弃置」）。 |
| PN-3 | **双状态轴**两行：行首轴名（规划轴 / 实现轴，52px）；节点胶囊 = 状态名 + mono 计数，按状态着色（构想 neutral / 成纲 info / 备写 accent；待写 faint / 撰写中 warn / 已完成 success / 弃置 faint），节点间 arrowRight 连接。 |
| PN-4 | 轴下方 warn 横幅：受阻单元汇总「另有 N 单元受阻：{单元}（{原因}）」。 |
| PN-5 | **大纲进度**：按层级缩进的行（深度 16px/级）：单元名 + 42px mini 进度条（success 填充）+ c/t mono + 规划/实现双 chip；点击行跳大纲并 toast「已定位到大纲：{单元}」。 |

### 8.2 待办详情
| 编号 | 需求 |
| --- | --- |
| PN-6 | 待办数据：`tag ∈ {approval 审批/shieldCheck/warn, profile 档案/userRound/info, writing 写作/feather/accent, decision 决策/gitBranch/neutral}`；`status ∈ open / done`；`action? {label, kind}`。 |
| PN-7 | 详情卡：分组 chip + 状态 chip（待处理 warn / 已完成 success）+ 标题（17 粗）+ mono 元信息 + 描述 + 动作区。 |
| PN-8 | 动作按 `kind`：`approval`→主钮「去审批」（跳对话 + 开审批面板）；`location`→「去完善」（跳内容·地点定位）；`chapter`→「去处理」（跳正文定位）；`chat`→「去讨论」；一律附 ghost「标记完成」（toast 反馈）。已完成待办显示 ghost「标记为未完成」。标记完成后若正选中该待办则回总览。 |

---

## 9. 审批面板（AP）

### 9.1 绑定与开合
| 编号 | 需求 |
| --- | --- |
| AP-1 | **与对话视图绑定**：仅对话视图显示；切到内容 / 计划视图自动隐藏（记忆开合状态），回到对话恢复。现有自动开合规则保留：新 pending 出现自动展开、pending 清零自动收起。 |
| AP-2 | 面板头：「审批」+ 待审计数徽标（warn 底 warn 字；零时 neutral faint 变体；数字变化 cnt-pop 弹跳）+ 说明「当前对话 · 一次调用一批」+ 模拟新审批（仅 demo）+ 关闭钮。 |
| AP-3 | 只显示**当前会话**的审批；一次工具调用产生的一批审批**纵向堆叠为卡片流**（间距 14），**无目录列**（一次 call 一批一起审）。 |

### 9.2 卡片结构（自上而下）
| 编号 | 需求 |
| --- | --- |
| AP-4 | 身份行：mono 工具名 + 状态 pill（待批准 warn / 已批准 success / 已拒绝·已过期 danger / 已修改 info）。 |
| AP-5 | 标题行：操作符 + 标题。操作符：增 `+`（success）/ 编 `~`、删 `−`（danger）/ Exit `»`（accent-ink）。 |
| AP-6 | **「当前内容」区（必须展示既有数据）**：<br>· **编辑** → 「当前内容 · 将被覆盖」，warn 3px 左线 + warn 5% 底；正文类显示修改前原文（衬线体），字段类显示 名称/简介/现状 等 dl；<br>· **删除** → 「当前内容 · 将被删除」，danger 左线 + danger 5% 底，列出将被删除的全部既有条目；<br>· **写入遇同名/既有数据** → 「当前内容 · 已存在同名数据」，展示既有字段；<br>· **纯新建** → 虚线左线容器 +「无既有数据 · 此操作为新建」。 |
| AP-7 | **变更区**（节头按操作命名：写入内容 / 变更后 / 删除参数）：操作色带（增 success 带 / 删·编 danger 带，`+/~/−` 符号 + 工具名）+ 参数 dl（标签列 76px）+（基线版本不符时）「版本已过期：正式稿已被其他修改更新，批准后此操作可能执行失败」warn 横幅。 |
| AP-8 | **ExitComposeMode 特殊卡片**：身份行「退出设计模式 · ExitComposeMode」；标题固定「**提交设计草稿**」；正文区「设计草稿 · design.md（全文）」= 品牌渐变描边文档视图（衬线 h5 标题 + `<b>标签</b>引导的段落/列表：目标 / 结构 / 改动 / 风险）。不渲染 当前内容/变更区 两段式。 |
| AP-9 | 操作区（仅 pending）：**批准**（主钮）/ **拒绝**（ghost danger）/ **请求修改**（ghost）；「请求修改」展开 0fr↔1fr 意见 textarea（0.25s）+ 取消 / 提交修改意见（空文案禁用）；意见按组独立保存。 |

### 9.3 决策与动效
| 编号 | 需求 |
| --- | --- |
| AP-10 | 决策后：卡片切换为状态横幅「已处理 · {状态}」；卡片以结果状态色播放 0.6s 脉冲（status-pulse）；计数徽标即时更新。 |
| AP-11 | 新审批入列：插入卡片流顶部，appr-in 0.35s 下滑入场 + accent 微光；计数 cnt-pop。 |
| AP-12 | 状态机沿用：pending → approved / rejected / edited（请求修改提交）/ expired。 |

---

## 10. 主题与设计令牌（TH）

| 编号 | 需求 |
| --- | --- |
| TH-1 | 分层机制不变：L2 设计语言（字体/字号/字重/圆角/时长/缓动）全局唯一；L3 语义色由 `<html data-theme="…">` 覆盖（仅 `--color-* / --shadow-*`）；语义混合色（`accent-9 / accent-8-t / danger-12 / chrome-bg / focus-ring / selection / scrollbar-thumb` 等）引用基色自动重 derive；组件零改动。 |
| TH-2 | 四主题：**宣纸白**（默认，暖白 + 朱砂橙红渐变）/ **墨夜**（暖黑 + 朱砂提亮，暗色）/ **黛青**（冷调蓝黑 + 青瓷，渐变换青蓝族）/ **雪青**（冷白瓷 + 群青，渐变换蓝紫族）。 |
| TH-3 | 暗色主题必须覆盖 `--color-on-accent` 为深墨色；`--grad-warn-text` 为固定美术资产各主题沿用。 |
| TH-4 | 字体栈：UI（系统 + PingFang/雅黑…）、正文衬线（Iowan/宋…）、楷体（LXGW/楷体）、mono（SF Mono/Consolas…）。核心字号：12 / 12.5 / 13 / 对话 15.5 / 正文 16.5；圆角 6/9/14/pill。 |
| TH-5 | 主题切换：挂 `html.theming` 类做 0.35s 全局颜色过渡（背景/边框/文字/阴影/描边），完成后移除；选择持久化。 |
| TH-6 | 所有新组件**只允许使用语义 token**，禁止硬编码 hex（WC-1 的 `#e81123`、WC-2 红绿灯三色为平台规范例外）。 |

## 11. 响应式与窗口尺寸（RWD）

断点对齐 `tokens.css`：**1280 / 1080**，以应用框架为容器（容器查询）；**应用最小窗口 1080**（Electron `minWidth: 1080 / minHeight: 640`，初始 `1280×800`）：

| 编号 | 条件 | 行为 |
| --- | --- | --- |
| RWD-1 | >1280 | 侧栏 272px；审批面板 376px，常驻右栏。 |
| RWD-2 | ≤1280 | 侧栏 238px；审批面板 340px；`paneInner` 边距收敛（22px）。 |
| RWD-3 | ≤1080（最小档） | 审批面板脱离文档流 → 右缘覆盖抽屉（`min(400px, 86cqw)`，`shadow-2`，关闭态 `translateX(103%)`）；打开时显示 `fg 14%` 遮罩，点遮罩关闭。 |
| RWD-4 | 窗口下限 | **最小窗口 1080×640（Electron minWidth/minHeight），初始 1280×800**；不提供 860 级移动布局（无侧栏抽屉、无标题栏图标化降级）。侧栏显隐仅手动开关（负 margin 收起，全宽度档一致）。 |
| RWD-5 | 任意（演示） | 窗口宽度模拟（demo 控制条）：满 / 1440 / 1280 / 1080 四档，居中呈现 + 环境底色 + 圆角投影（仅演示）。 |
| RWD-6 | 验收下限 | 1080px 宽下三视图全部可用、无横向滚动、目录可读（截断省略号）。 |

## 12. 动效规范（AN）

| 名称 | 参数 | 用途 |
| --- | --- | --- |
| grad-flow | 6s 循环 | 品牌圆点 / 草稿 kicker / 状态文字渐变流动（状态行 4.5s） |
| msg-enter | 0.22s ease-out，上移 6px | 消息进场 |
| view-in | 0.3s ease-out，上移 6px | pane / 弹窗 / 菜单（菜单 0.18s） |
| spinner-rotate | 0.8s linear | 工具行进行中 |
| draft-cursor | 1.1s steps(2) | 草稿光标闪烁 |
| node-pulse | 2.4s | 进行中秒数脉动 |
| status-bob / sway / drop | 1.5s / 2.8s / 1.2s | 生成态笔尖浮动 / 审批态沙漏摇摆 / 墨滴下落 |
| failed-shake | 0.45s 一次性 | 失败图标抖动 |
| appr-in | 0.35s 下滑 + accent 微光 | 新审批卡片入列 |
| status-pulse | 0.6s（注入 `--pulse-c`） | 审批决策脉冲 |
| cnt-pop | 0.3s 缩放弹跳 | 计数变化 |
| 0fr↔1fr grid | 0.3s / 0.25s | 状态行 / 请求修改展开 |
| 主题过渡 | 0.35s | 见 TH-5 |

保留全局 `prefers-reduced-motion` 降级（现有 `global.css`）。

## 13. 状态语义表（全局统一）

| 状态 | chip/badge 类 | 底/字 |
| --- | --- | --- |
| 构想 / 待写 | neutral | surface-2 / muted |
| 成纲 | info | info-bg / info |
| 备写 | accent | accent-11 / accent-ink |
| 撰写中 / 生成中 / 待处理 | warn | warn-bg / warn |
| 已完成 / 已批准 | success | success-bg / success |
| 受阻 / 失败 / 已拒绝 / 已过期 | danger | danger-bg / danger |
| 弃置 | faint | 透明 + border / faint |
| 已修改（审批） | info | info-bg / info |

## 14. 可访问性（A11Y）

| 编号 | 需求 |
| --- | --- |
| A11Y-1 | 语义与状态：视图 tab `role="tablist"` + `aria-selected`；模式钮 `aria-expanded`、模式菜单 `role="listbox"`；chips/toggles 用 `aria-pressed / aria-checked`；对话流容器 `role="log"`。 |
| A11Y-2 | 隐藏面板（审批收起 / 侧栏收起）置 `aria-hidden` + `inert`，不参与焦点序。 |
| A11Y-3 | 全部可交互元素 focus-visible 焦点环（`--color-focus-ring`，offset 1–2px）。 |
| A11Y-4 | 键盘：Enter 发送 / Shift+Enter 换行 / IME 组合态不触发；Esc 关闭弹窗与下拉（模式菜单 / 工作区菜单 / 设置）。 |
| A11Y-5 | 文案对比度满足现有纪律测试（亮暗主题分别校验）。 |

## 15. 验收标准（对齐 demo 冒烟 56 项，按域归并）

**外壳**：三视图 tab 与滑块；Win 三窗控（关闭 hover 红）；mac 红绿灯且互斥；双击标题栏最大化切换；关闭出现退出遮罩、可重新打开；工作区菜单可切换并更新 chip + toast。
**侧栏**：随视图切换三种目录；大纲树为嵌套层级 + 引导线 + 叶节点短线；展开/折叠全部生效；卷组折叠生效。
**对话**：输入框与底栏（模式左 / 发送右）；模式下拉 3 项含图标名称说明；选择后显示「待生效」（无模式名）且不丢输入草稿；发送追加用户气泡与演示回复；生成中发送→暂停互换；状态三态：正在生成 / **正在审批** / 失败（重试）；Enter/Shift+Enter/IME。
**内容**：四资料位切换；大纲单元详情（chips/进度/受阻/弃置/关联）；正文目录 + 阅读区（草稿块标记、受阻横幅、未开笔空态）；人物/地点目录与档案、关联跳转；单元详情→正文跳转。
**计划**：6 统计卡；双状态轴；大纲进度行点击定位；待办详情跨视图动作（去审批/完善/处理/讨论）+ 标记完成。
**审批**：与对话视图绑定（切内容/计划隐藏、回来恢复）；一批卡片堆叠无目录；Exit 卡片（固定标题 + design.md 全文 + `»`）；编辑显示「当前内容 · 将被覆盖」原文；删除显示将被删除的既有数据；写入遇同名展示既有档案、纯新建标注「无既有数据」；批准/拒绝/请求修改（含意见展开与禁用态）→ 横幅 + 脉冲 + 计数更新；新审批入列动画。
**系统**：四主题即时切换（含设置卡入口）与过渡；窗口宽度四档模拟（满 / 1440 / 1280 / 1080）；≤1080 降级路径（审批右缘覆盖抽屉 + 遮罩）；最小窗口 1080 下三视图可用；toast 2.2s 自动消失。

## 16. 实施映射（demo → 现有 `ui/src`）

| demo 区域 | 现有模块 | 改造类型 |
| --- | --- | --- |
| 侧栏（上下文目录） | Sidebar + ContentSection / ConversationListSection | 改造：内容随视图切换；新增资料位 segTabs / 安排目录 |
| 标题栏 + 窗控 | TopBar / TopBarViewSwitcher（drag 区已预留） | 扩展：自绘窗控（`extensions.titleBar` 槽）、工作区下拉 |
| 对话流 | ChatSurface / ConversationTimeline / AssistantMessage / NovelDraftPanel | 保留；新增轮次分隔条、审批系统消息样式 |
| 输入区 | ConversationComposer / ComposerModeBar / GenStatus | 改造：模式下拉移至底栏 + 待生效 chip；恢复暂停钮；挂起文案改「正在审批」 |
| 内容视图 | ContentSurface + 四 pane | 改造：StoryOutlineTree 嵌套 + 引导线 + 详情区；ManuscriptReader 保留双栏；Character/LocationGrid → 左目录 + 右档案 |
| 计划视图 | ScheduleSurface | 改造：单列 → 左目录 + 右总览/详情；待办动作跨视图化 |
| 审批面板 | InspectorHost / ApprovalPanel | 改造：去目录抽屉 → 卡片流；新增 当前内容 / Exit 视图；绑定对话视图；仍复用 ApprovalStore |
| 主题/断点 | tokens.css / ThemeProvider / global.css | 复用（主题已定稿）；新增容器查询断点样式 |
| 窗控 IPC | gui/src/main/minimal.ts | 新增：frameless + 窗控 IPC + 平台判定 + 最小窗口 1080×640（初始 1280×800） |

## 17. 决议（2026-08-15，按推荐方案定案；最小窗口 1080）

| # | 原问题 | 决议 |
| --- | --- | --- |
| 1 | InspectorHost 还承载 档案 / 大纲单元 / 会话元信息 面板，去向待定 | 档案与大纲详情已移入内容视图主区；**会话元信息并入对话视图 subHead「更多」菜单**（「会话信息」轻量弹出）；InspectorHost 仅承载审批。 |
| 2 | 侧栏 / 审批面板拖拽调宽（现网 232–420 / inspector DragHandle）是否保留 | **不保留拖拽**，移除 DragHandle；宽度随断点固定档位（侧栏 272/238；审批 376/340、≤1080 抽屉 `min(400px, 86vw)`）。**v0.8 修订**：右栏改内容目录后恢复拖宽（见决议 7）。 |
| 3 | 会话搜索 / 会话更多菜单（重命名·置顶·删除）/「打开其他项目…」完整交互 | 本轮不重设计，**按现网行为保留**。 |
| 4 | 设置「通用」页配置键与持久化 | 三个开关落地为 `startup.reopenLastWorkspace`（启动打开上次工作区）/ `editor.serifBody`（正文衬线字体）/ `chat.showToolElapsed`（生成中显示工具行耗时），localStorage 持久化。 |
| 5 | Electron frameless 系统级细节 | Windows `frame:false` 全自绘；macOS `titleBarStyle:"hidden"` 保留系统红绿灯（`trafficLightPosition` 定位）；**不使用** `titleBarOverlay`（自绘一致性）；标题栏整栏 drag、按钮/菜单/switcher 均 no-drag（TB-1 / WC-5）。 |
| 6 | 是否需要 ≤860 移动级布局 | **不做**；应用**最小窗口 1080×640**（初始 1280×800），断点收敛为 1280 / 1080 两档，demo 已移除 860 档与侧栏抽屉 / 标题栏图标化降级（RWD-4）。 |
| 7 | （2026-08-16 v0.8，demo 方案 A 已实施）审批交互、右栏去向、实体标签与 md 排版 | ① **审批改整体弹窗**（阻塞式：左清单+右详情+全部批准+稍后处理；稍后处理由顶部提示条/状态行/工具行/系统行唤回；处理完不留过往记录），InspectorHost 不再承载审批；② **右栏 = 内容目录**（大纲/人物/地点三 tab；行点击手风琴详情卡 +「打开完整档案」跳内容视图；进入 chat 默认展开、顶条开关可收起）；③ **实体标签定位优先**：chat 视图点击 character/location chip → 右栏定位，非 chat 直跳；④ markdown 全格式渲染补齐（代码块头+复制/任务清单/脚注/图片占位/行内强调族）；⑤ 右栏恢复拖宽 320–640（双击复位；≤1280 固定档，决议 2 修订）。原型：`docs/design/approval-modal-demo.html`（A，已实施）/ `approval-inline-demo.html`（B，落选留档）。 |

---

*附：demo 快速对照——打开 `docs/design/app-redesign-demo.html`，底部控制条可切 主题 / 平台窗控 / 窗口宽度 / 运行状态 / 审批面板 / 说明；「说明」弹层即本 PRD 要点速览。*
