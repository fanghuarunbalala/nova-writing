# OverflowTestTool

- **工具名**: `OVERFLOW_TEST_TOOL_NAME`（导出值为空字符串 `''`；无 buildTool 定义、无 userFacingName）
- **源码**: `vendor/claude-code/packages/builtin-tools/src/tools/OverflowTestTool/`
- **门槛**: `feature('OVERFLOW_TEST_TOOL')`（`vendor/claude-code/src/tools.ts` getAllBaseTools 中 `...(OverflowTestTool ? [OverflowTestTool] : [])`）

## 描述（模型侧 desc）

无。该目录仅含一个自动生成的占位文件（`OverflowTestTool.ts`），其完整内容为：

```ts
// Auto-generated stub — replace with real implementation
export {}
export const OVERFLOW_TEST_TOOL_NAME: string = ''
```

无 `prompt.ts`、无 `inputSchema`、无 `buildTool`。

## Input Schema

无（未定义）。
